/*
 * O padrão Decorator permite que você adicione funcionalidades a um objeto dinamicamente, 
 * sem alterar a estrutura do objeto. Esse padrão é útil quando você deseja estender as 
 * funcionalidades de classes de maneira flexível e reutilizável.
 */

// Interface Coffee que define o método makeCoffee()
public interface Coffee {
    String makeCoffee();
}

